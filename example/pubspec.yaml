name: example
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:

dev_dependencies:
  build_runner:
  bug_generator:
    path: ../

